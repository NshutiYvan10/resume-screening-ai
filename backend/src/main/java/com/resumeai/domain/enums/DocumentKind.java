package com.resumeai.domain.enums;

/**
 * What a saved candidate FILE is. Cover letters are deliberately absent: they are
 * saved as text templates (see CoverLetterTemplate), not files.
 */
public enum DocumentKind {
    RESUME
}
